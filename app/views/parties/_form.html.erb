<%= form_with(model: party, local: true) do |form| %>
  <% if party.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(party.errors.count, "error") %> prohibited this party from being saved:</h2>

      <ul>
      <% party.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :party_name %>
    <%= form.text_field :party_name %>
  </div>

  <div class="field">
    <%= form.label :party_code %>
    <%= form.number_field :party_code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
